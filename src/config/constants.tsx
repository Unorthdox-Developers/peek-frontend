export const Strings = {
  default: {
    repositorySearch: {
      placeholder: 'Search Repository',
      buttonText: 'Search',
    },
  },
};

export enum AsyncTrunkRequestStatus {
  Initial = 'initial',
  Pending = 'pending',
  Fulfilled = 'fulfilled',
  Rejected = 'rejected',
}
