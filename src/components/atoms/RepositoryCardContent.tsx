import { CardContent } from '@material-ui/core';

export type RepositoryCardContentProps = {};

const RepositoryCardContent = (props: RepositoryCardContentProps) => {
  return <CardContent></CardContent>;
};

export default RepositoryCardContent;
